hydra:
  searchpath:
    - file://external/verl/verl/trainer/config

defaults:
  - ppo_trainer
  - _self_

data:
  max_prompt_length: 1024
  max_response_length: 1024
  train_batch_size: 256
  val_batch_size: 256
  return_raw_chat: True

actor_rollout_ref:
  hybrid_engine: True
  actor:
    loss_agg_mode: seq-mean-token-mean
    entropy_mask:
      mode: null
      raw: null
    # 移除旧的 advantage 配置，统一到 semantic_advantage
  rollout:
    name: vllm
    mode: async
    multi_turn:
      completion_callback: beyondagent.module.trainer.simple_completion_callback.SimpleCompletionCallback
      enable: True
      format: llama3_json
      max_steps: 30
      tool_config_path: ""
    custom_dataflow_cls:
      path: ""
      name: ""
    use_qwen3: True
    max_env_worker: 64
    max_env_len: 4096
    enable_request_id: False

thread_pool:
  max_workers: 5

env_service:
  env_type: "appworld"
  env_url: "http://127.0.0.1:8000"

experience_maker:
  base_url: "http://127.0.0.1:8001"
  workspace_id: "w1_agentflow"
  enable_summarizer: False
  enable_context_generator: False
  retrieve_top_k: 3

save_dir: ./save_dir/save_entropy

# 统一的语义优势评估配置 - 所有语义评估相关参数都在这里
semantic_advantage:
  enable: false                    # 总开关
  evaluation_type: "local"           # 🔥 新增：评估类型 ("local" 或 "api")
  mask_type: "loss_mask"          # mask类型
  mode: "semantic"                 # 模式
  good_scale: 1.0                  # 好步骤缩放因子
  bad_scale: 0.2                   # 坏步骤缩放因子
  neg_bad_scale: -0.2              # 负面坏步骤缩放因子
  concurrent: 20                   # 并发处理数量
  model: "qwen-max"              # 语义评估使用的模型
  api_max_retries: 200             # API调用最大重试次数，默认200次